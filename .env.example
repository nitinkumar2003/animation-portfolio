# Server-only. Never prefix this key with NEXT_PUBLIC_.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6-luna

# Set this to the deployed portfolio origin for canonical SEO URLs.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
